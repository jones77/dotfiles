#!/usr/bin/env bash
# linux specific profile
col=$(tput cols)
lines=$(tput lines)
echo running .profile.gnulinux "($lines,$col)"

# linuxbrew
export PATH="$HOME/.linuxbrew/bin:$PATH"
export MANPATH="$HOME/.linuxbrew/share/man:$MANPATH"
export INFOPATH="$HOME/.linuxbrew/share/info:$INFOPATH"

